{
  "dependencies": {
    "apollo-codegen": "^0.19.1",
    "apollo-server-express": "^1.3.6",
    "express": "^4.16.3",
    "graphql": "^0.13.2",
    "graphql-tools": "^3.0.1",
    "mocha": "^5.1.1"
  },
  "main": "app.js",
  "scripts": {
    "test": "NODE_ENV=test mocha --no-timeouts --recursive --reporter xunit-file test --exit",
    "start": "node app.js"
  }
}
